import { useTranslation } from 'react-i18next';

const StudentLayout = () => {
  const { t } = useTranslation();
  return (
    <div>
      StudentLayout
    </div>
  );
};

export default StudentLayout;
